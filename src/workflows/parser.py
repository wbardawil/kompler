# MIT License — DocuVault AI
"""Parse YAML workflow definitions into WorkflowDefinition models.

Layer: Workflows
Phase 2 deliverable.
"""
# TODO: Implement:
# - parse_file(path) → WorkflowDefinition
# - parse_string(yaml_str) → WorkflowDefinition
# - Validate step types against WorkflowStepType enum
# - Validate trigger expressions (simple equality: "doc_type == 'supplier_certificate'")
